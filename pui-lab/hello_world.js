
/*let assisgmentScores = [30, 20, 50];

//totalScore = assisgmentScores[0] + assisgmentScores[1] + assisgmentScores[2];

//console.log(totalScore);

function calculateTotal(hw1, hw2, hw3) {
    let total = hw1 + hw2 + hw3;
    return (total);
}

let totalScore = calculateTotal(assisgmentScores[0], assisgmentScores[1], assisgmentScores[2]);
console.log(totalScore);

/*
let grade;

if (totalScore >= 92) {
    grade = "A";
}
else if (85 <= totalScore && totalScore < 92) {
    grade = "B";
} else {
    grade = "C";
}

console.log(grade);



for (let i=0; i<20;i++) {
    console.log("meow")
}

let i =0;
let text = "";

while (i<10) {
    text += "The number is" + i;
    console.log(text);
    i++;
}

*/